# CLAUDE.md

## このリポジトリの目的

このリポジトリは、仮称「idea-to-action」という Android 向け MVP アプリの開発用です。

アプリのコンセプト:
- 曖昧なアイデアや「やりたいこと」を膨らませる
- 具体的なプランに落とし込む
- スモールステップで着手しやすくする
- 忘却や着手時の摩擦を減らす

現時点の優先順位:
- まずは Android のみ
- Kotlin + Jetpack Compose
- NixOS 上の CLI 中心ワークフロー
- 通常開発では Android Studio 前提にしない
- 将来的に Linux desktop は検討するが、今は実装対象外

## リポジトリ構成

- `flake.nix`: Android 開発用 devShell
- `.envrc`: direnv 用設定
- `scripts/doctor.sh`: 開発環境確認
- `scripts/build-check.sh`: ビルド確認
- `android-app/`: Android アプリ本体

## 開発環境

開発は常に Nix の devShell 内で行う前提です。

作業前の原則:
1. 開発環境に入っていることを確認する
2. 既存の構成を尊重する
3. Nix ベースのワークフローと衝突するツールや手順を勝手に増やさない

## ビルドと確認

意味のある変更の前後では、以下を実行すること。

```bash
./scripts/doctor.sh
cd android-app
./gradlew --stop
./gradlew assembleDebug
```

テストが追加された場合は検証手順に含めてよいが、存在しないコマンドを勝手に仮定しないこと。

## 実装方針

アプリは小さなステップで段階的に作ること。

現在の優先順:
1. 常にビルド可能な状態を保つ
2. まず Inbox フローを作る
3. 次に ViewModel による最小の状態管理を入れる
4. その後に永続化を入れる
5. AI コーチング要素はその後でよい

## スコープ制約

当面の方針:
- Android のみ対象
- 単一 app module を優先
- 「理想的な設計」より「変更しやすい単純さ」を優先
- 依存関係は最小限にする
- 読みやすく、後から直しやすいコードを優先する

明示的な指示がない限り、以下を導入しないこと:
- Hilt などの DI フレームワーク
- 過剰な Clean Architecture
- 多モジュール化
- バックエンドサービス
- 分析基盤
- 認証
- 同期機能
- 必要になる前の複雑な Navigation

## UI 方針

当面は試作速度を優先する。

- Compose UI を使う
- 画面はシンプルに保つ
- スタイリングは最小限でよい
- 見た目より機能確認を優先する
- 早すぎるデザインシステム化はしない

## 状態管理方針

当面の推奨:
- 画面状態は ViewModel を使う
- state model は小さく保つ
- シンプルな Flow / StateFlow を優先する
- 明確に必要になるまで複雑なイベントシステムを作らない

## コア実装の方針

- UI から独立できるロジックは、できるだけ Android UI 層から切り離す
- コアロジックは将来的に CLI や別フロントエンドから再利用しやすい形を優先する
- Android 固有の依存は外側の層に閉じ込める
- ただし初期 MVP では過剰な抽象化や分離を行わず、必要になった範囲で段階的に切り出す

## 編集ルール

変更時の原則:
- 変更は最小の有用単位で行う
- 無関係なリファクタをしない
- 明示的に指示されない限り、大規模な rename をしない
- 既存のビルド成功状態を壊さない
- 迷ったら単純な案を選ぶ

## 提案の仕方

変更提案時:
- 具体的に書く
- 短い計画を優先する
- トレードオフは簡潔に説明する
- 明示的な要求がない限り、大規模書き換えを提案しない

実装時:
- どのファイルを変更するか最初に示す
- 差分は小さく保つ
- 一度に一つの小さな作業を優先する

## 開発者の好み

- 開発者は NixOS ユーザーで、ターミナル志向
- GUI 前提ではなく、コマンドとスクリプトを優先する
- 再現可能な手順を優先する
- 手でレビューしやすい差分を好む

## 直近の目標

次の意味あるマイルストーンは以下:
- 最小の Inbox 画面
- ViewModel を使った状態管理
- 短いアイデアを入力できる
- 入力した項目を簡単な一覧で表示できる
- ビルドが常に通る
