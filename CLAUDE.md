# CLAUDE.md

## このリポジトリの目的

このリポジトリは「idea-to-action」という Android 向けアプリの開発用です。

アプリのコンセプト:
- 曖昧なアイデアや「やりたいこと」を膨らませる
- 具体的なプランに落とし込む
- スモールステップで着手しやすくする
- 忘却や着手時の摩擦を減らす

現時点の優先順位:
- まずは Android のみ
- Kotlin + Jetpack Compose
- NixOS 上の CLI 中心ワークフロー
- 通常開発では Android Studio 前提にしない
- 将来的に Linux desktop は検討するが、今は実装対象外

## リポジトリ構成

- `flake.nix`: Android 開発用 devShell
- `.envrc`: direnv 用設定
- `scripts/doctor.sh`: 開発環境確認
- `scripts/build-check.sh`: ビルド確認
- `android-app/`: Android アプリ本体

## 開発環境

開発は常に Nix の devShell 内で行う前提です。

作業前の原則:
1. 開発環境に入っていることを確認する
2. 既存の構成を尊重する
3. Nix ベースのワークフローと衝突するツールや手順を勝手に増やさない

## ビルドと確認

意味のある変更の前後では、以下を実行すること。

```bash
./scripts/doctor.sh
cd android-app
./gradlew --stop
./gradlew assembleDebug
```

テストが追加された場合は検証手順に含めてよいが、存在しないコマンドを勝手に仮定しないこと。

## アプリの設計方針

### 価値観（優先順）

1. **着手ファースト** — 完了できなくてもいい。始めることが目標
2. **AIが候補を出す** — ゼロから考えさせない。承認するだけにする
3. **ゆるく続ける** — 完璧なレビューより、続けられる軽さを優先
4. **リストを増やさない** — たくさん並べると着手しなくなる

### フロー構造

```
アイデアを入力（アイデアタブ）
  ↓ AIと対話してテーマ化（InboxDetailのBottomSheet内で完結）
テーマ（個人プロジェクト。タイトル・ゴール・重さ）
  ↓ ステップを作成
ステップ（テーマに紐づく具体的な行動単位）
  ↓ 「今日やる」に追加（ホームの追加ボタンから。2タップ）
ホーム（今日やること）
  ↓ 着手マーク（タップ1回）
振り返り（ホームに夜の時間帯で統合。3択: できた / 着手した / できなかった）
```

### ナビゲーション構成（BottomNav 3タブ）

```
ホーム | アイデア | 設定
```

- **ホーム**: 今日やること（順序付きリスト）+ 夜は振り返りカード
- **アイデア**: Inbox（入力フォーム + 未整理メモ一覧）
- **設定**: APIキー管理 + テーマ一覧へのリンク

振り返り・テーマ一覧は専用のBottomNavタブを持たない。

### 時間管理の方針

**特定時刻のスケジューリングはしない。**

`startTime` / `duration` フィールドは使用しない。時刻による管理は着手ファーストの価値観と矛盾し、守れなかった時の失敗感を生む。

代わりに、今日のステップは **順序付きリスト** で管理する。並び順（`sortOrder`）で「これを先にやる」を表現し、上から順に着手していく。

### AIコーチングの優先順位

| 優先度 | コンテキスト | 用途 |
|--------|-------------|------|
| 最高 | MORNING_SUGGEST | 朝の起動時に自動実行。今日のステップ候補を提案 |
| 高 | LAUNCH | Inbox 1件 → テーマ化の対話 |
| 中 | INBOX_BATCH | 複数 Inbox エントリをまとめてテーマ化 |
| 低 | REVIEW | 振り返りフィードバック + 次ステップ提案 |
| 低 | THEME_FOCUS | テーマが増えすぎた時の絞り込み（当面は不要） |

APIキー未設定時はAI機能をすべてスキップし、手動フローで動く。

### やらないこと（明示的に封印）

- 特定時刻のスケジューリング（startTime / duration の UI 露出）
- タイムライン表示（時刻あり / 時刻なし の分割セクション）
- 通知の詳細設定UI（振り返りリマインダー1本のみ、設定固定）
- ステップの「重さ」をユーザーに見せること（AIの内部判断用）
- バッジ・ストリーク・ゲーミフィケーション
- チーム共有・コラボ機能
- カレンダー連携・ルーチン管理

## 現在の実装状態

### 実装済み

- **データモデル**: InboxEntry / Theme / Step / ScheduledStep / CoachingMessage（5テーブル）
- **ホーム（今日やること）**: 今日/明日タブ、着手マーク、詳細BottomSheet、MorningSuggest BottomSheet
- **Inbox**: テキスト入力、一覧、選択モード、バッチコーチングへの導線
- **テーマ一覧**: アクティブ/アーカイブ切替
- **テーマ詳細**: ステップ一覧
- **ステップ詳細**: タイトル・重さ・スターターアクション
- **振り返り**: 3択UI（できた/着手した/できなかった）、明日持ち越し
- **設定**: Claude APIキー管理
- **AIコーチング**: チャットUI（LAUNCH / INBOX_BATCH / MORNING_SUGGEST / REVIEW / THEME_FOCUS）
- **MorningSuggestViewModel**: ステップ候補提案（UIはBottomSheetとして実装済み）

### 既知の問題・未対応

- **G-2**: テーマ詳細に実績ログがない（完了ステップ数 / 総数の表示が未実装）
- **G-4**: 着手済み未完了ステップの翌日持ち越しが手動のみ（自動化未実装）
- **ナビゲーション**: BottomNavが5タブ（今日/Inbox/テーマ/振り返り/設定）のまま。3タブへの整理が未対応
- **振り返り統合**: 振り返りが独立タブのまま。ホームへの統合が未対応
- **MorningSuggest 自動実行**: 起動時の自動トリガーが未実装
- **CoachingScreen 統合**: 独立画面のまま。InboxDetail BottomSheet への統合が未対応
- **並び順管理**: `sortOrder` カラムが未実装。ドラッグ並び替えも未対応
- **REVIEW コーチング**: 提案されたステップをテーマに追加する機能がない
- **ThemeFocus**: ViewModel はあるが UI ボタンが未接続

## 直近の目標

優先順に並べる。依存関係を考慮して上から順に対応する。

### P1：コアフロー修正

1. ナビゲーションを 5タブ → 3タブ（ホーム / アイデア / 設定）に整理
2. テーマ一覧を設定画面から到達できるようにする
3. 振り返り画面をホームに統合（夜の時間帯でカードを表示）

### P2：ホーム画面の強化

4. MorningSuggest を起動時に自動実行する
5. 空のホームから「+ やること追加」→ ステップ選択 → 今日に追加 を 2タップで完結させる
6. `sortOrder` カラム追加と並び順管理の実装

### P3：体験の磨き込み

7. starterAction をホーム画面のリストに表示する
8. テーマ詳細に「完了ステップ数 / 総数」を表示する
9. 着手マーク時のマイクロアニメーション

### P4：AI体験の強化

10. REVIEW コーチングの提案ステップをテーマに追加する機能
11. G-4：着手済み未完了ステップの翌日自動持ち越し

## スコープ制約

当面の方針:
- Android のみ対象
- 単一 app module を優先
- 「理想的な設計」より「変更しやすい単純さ」を優先
- 依存関係は最小限にする
- 読みやすく、後から直しやすいコードを優先する

明示的な指示がない限り、以下を導入しないこと:
- 過剰な Clean Architecture
- 多モジュール化
- バックエンドサービス
- 分析基盤
- 認証
- 同期機能
- 必要になる前の複雑な Navigation

## UI 方針

当面は試作速度を優先する。

- Compose UI を使う
- 画面はシンプルに保つ
- スタイリングは最小限でよい
- 見た目より機能確認を優先する
- 早すぎるデザインシステム化はしない

## 状態管理方針

当面の推奨:
- 画面状態は ViewModel を使う
- state model は小さく保つ
- シンプルな Flow / StateFlow を優先する
- 明確に必要になるまで複雑なイベントシステムを作らない

## コア実装の方針

- UI から独立できるロジックは、できるだけ Android UI 層から切り離す
- コアロジックは将来的に CLI や別フロントエンドから再利用しやすい形を優先する
- Android 固有の依存は外側の層に閉じ込める
- ただし初期 MVP では過剰な抽象化や分離を行わず、必要になった範囲で段階的に切り出す

## 編集ルール

変更時の原則:
- 変更は最小の有用単位で行う
- 無関係なリファクタをしない
- 明示的に指示されない限り、大規模な rename をしない
- 既存のビルド成功状態を壊さない
- 迷ったら単純な案を選ぶ

## 提案の仕方

変更提案時:
- 具体的に書く
- 短い計画を優先する
- トレードオフは簡潔に説明する
- 明示的な要求がない限り、大規模書き換えを提案しない

実装時:
- どのファイルを変更するか最初に示す
- 差分は小さく保つ
- 一度に一つの小さな作業を優先する

## 開発者の好み

- 開発者は NixOS ユーザーで、ターミナル志向
- GUI 前提ではなく、コマンドとスクリプトを優先する
- 再現可能な手順を優先する
- 手でレビューしやすい差分を好む
