# idea-to-action 設計レビュー

2026-04-16 作成。ヒアリング結果と現行コードベースに基づく。

---

## 1. 現状の問題整理

### 1.1 設計が二重に存在している

コードベースに **2つの異なる設計思想** が混在している。

| | コード実装（CLAUDE.md） | ドキュメント（app-spec.md） |
|---|---|---|
| データ単位 | Node（汎用） | Inbox → Theme → Step → ScheduledStep |
| 関係管理 | Edge（手動DAG） | テーマ配下のステップ階層 |
| 今日の表示 | READY ノード一覧 | AI提案 + スケジュール + 通知 |
| 整理方法 | Graph タブでエッジ操作 | AI対話でテーマ化 |
| 振り返り | なし | 3択評価 + AI FB |

この二重性が「作り直しているうちに迷走する」の直接原因。
どちらの設計も一長一短があるが、**どちらも完成に至っていない**。

### 1.2 Node = 万能エンティティ の限界

現行コードの `NodeEntity` は「アイデア」も「具体的な行動」も同じテーブル・同じステータスフローで扱う。

```
IDEA → READY → ACTIVE → DONE
```

しかし実際の使用では:
- **アイデア**（例: 「副業について考える」）は完了しない。育てるもの。
- **行動**（例: 「競合3社をリストアップする」）は完了する。消化するもの。

これらを同一の `status` で管理すると:
- IDEA なのか、ステップが見えていない行動なのか区別がつかない
- リストが IDEA と行動で混在し、「何から手をつけるか」がぼやける
- 結果、**Todayビューを見なくなる**（= 実際に起きた現象）

### 1.3 DAG/Edge による整理は使われなかった

Graph タブのエッジ管理は Zettelkasten 的な発想で設計上は美しいが:
- モバイルで「ノードをタップ → 前提を選択 → エッジタイプを指定」は **操作コストが高すぎる**
- 実際にほとんど使われなかった（ヒアリングで確認済み）
- READY 判定（全前提が DONE）は DAG があって初めて動くが、エッジを張らないとすべてのノードが即 READY になり、判定の意味がなくなる

**投下したコードの複雑さに対して得られた価値がない** 状態。

### 1.4 app-spec.md は正しい方向だが重すぎる

docs/app-spec.md の設計はユーザーの目標に近いが:
- ScheduledStep（日次スケジュール管理）は TaskChute 寄りで **負荷が高い**
- 通知・時刻管理は「気分で取り組みたい」というスタイルに合わない
- AI の4つの役割は理想だが、**1つも実装されないまま設計だけ肥大化** している
- 4タブ → 3タブの変更が入り、さらに迷いが深まっている

---

## 2. 核心的な課題

**「アイデアの捕捉」と「行動の管理」を同じ粒度・同じ仕組みで扱おうとしている。**

これが設計の迷走を生んでいる。アイデアは発散するもの、行動は収束するもの。
同じエンティティ（Node）で両方を表現しようとすると、どちらにも最適化できない。

| | アイデア | 行動（ステップ） |
|---|---|---|
| 性質 | ふわっとしている | 具体的 |
| 量 | 増えてよい | 絞る |
| 操作 | 育てる、整理する、プロジェクト化する | 着手する、完了する |
| 完了 | しない（プロジェクトに統合される） | する |

---

## 3. 提案する設計方針

### 3.1 設計原則

1. **アイデアとアクションを明確に分離する**
2. **構造化は手動 DAG ではなく、プロジェクト配下のリストで表現する**
3. **日次の運用は「選ぶ→やる→記録する」の3ステップに絞る**
4. **AI は提案役。操作の主導権はユーザーに残す**

### 3.2 データモデル

```
Memo（旧 Node の「アイデア」側）
  id: Long
  text: String
  createdAt: Long
  projectId: Long?     // プロジェクトへの紐付け（整理済みフラグも兼ねる）

Project（旧 Theme を拡張）
  id: Long
  title: String
  goal: String?        // 任意。書けると方向が定まる
  status: active | paused | archived
  focusedAt: Long?     // 「今これに集中」のタイムスタンプ
  createdAt: Long

Step（プロジェクト配下の具体的な行動）
  id: Long
  projectId: Long
  title: String
  sortOrder: Int       // 表示順
  status: pending | done
  doneAt: Long?
  createdAt: Long

DailyLog（やったことの記録）
  id: Long
  date: String         // "2026-04-16"
  stepId: Long?        // ステップ経由の記録は紐付け
  what: String         // やったことの一行要約
  note: String?        // 任意メモ
  createdAt: Long
```

**現行コードからの差分:**

| 現行 | 提案 | 理由 |
|------|------|------|
| Node | Memo + Step に分離 | アイデアと行動を混ぜない |
| Edge | 削除 | 使われなかった。プロジェクト階層で代替 |
| Theme | Project に拡張 | goal, status, focus を追加 |
| NodeStatus (6種) | Memo: 紐付けの有無, Step: pending/done | ステータスマシンを単純化 |
| — | DailyLog 新設 | 軽い振り返りの受け皿 |

### 3.3 画面構成（3画面 + BottomNav）

```
Today | Capture | Projects
```

#### Today（起動時デフォルト）
- **フォーカス中プロジェクト** の pending ステップを表示
- ステップの「着手」→「完了」をワンタップ
- 完了時に DailyLog が自動生成される（what = ステップタイトル）
- 画面下部に「今日の記録」セクション（DailyLog 一覧）
  - ステップ外の自由記録も追加可（「散歩した」など）
- フォーカス中プロジェクトがない場合: 「どのプロジェクトに集中する？」を促す

#### Capture（メモ投入）
- テキスト入力 → Memo として保存（現行 Capture と同等。ここは機能している）
- 未整理メモ一覧
- メモタップ → 「プロジェクトに紐付ける」or「新規プロジェクト作成」
- AI: 「このメモは○○プロジェクトに関連しそう」と候補を出す（現行の関連提案を流用可能）

#### Projects（整理用）
- アクティブなプロジェクト一覧
  - フォーカス中のプロジェクトを上部に強調表示
- プロジェクトタップ → 詳細:
  - ゴール表示・編集
  - ステップ一覧（追加・並び替え・完了マーク）
  - 紐付いたメモ一覧（元のアイデアを参照可能）
  - 実績ログ（DailyLog からフィルタ）
- 「フォーカスする」ボタン（1-2個に絞る想定）
- 「休止する」「アーカイブする」操作

### 3.4 AI の役割（絞り込み）

現行の3つの AI 機能を以下に再整理:

| 優先度 | タイミング | 内容 | 実装難度 |
|--------|-----------|------|----------|
| 高 | Capture 時 | メモ → 既存プロジェクトとの関連候補 | 低（現行流用） |
| 中 | Today 表示時 | 「今日はこのステップが良さそう」 | 低（現行流用） |
| 低 | Projects 一覧 | 「○○が放置されています。休止しますか？」 | 中 |

**削除する AI 機能:**
- Edge 提案（Edge 自体を廃止するため）
- テーマ名提案（プロジェクトは自分で名前をつける）
- 振り返り AI フィードバック（過剰。ログだけで十分）
- ステップ具体化 AI（ヒアリングで「微妙」との評価）

### 3.5 日次フロー

```
朝（または気が向いたとき）
  ↓ アプリを開く → Today 画面
  ↓ フォーカス中プロジェクトの次ステップが見える
  ↓ 「これやろう」→ ステップ完了 → DailyLog 自動記録
  ↓
思いついたとき
  ↓ Capture タブ → メモ投入（10秒で終わる）
  ↓
気が向いたとき
  ↓ Capture タブ → 溜まったメモを整理（プロジェクトに紐付け or 新規作成）
  ↓ Projects タブ → ステップを追加、フォーカスを切り替え
  ↓
寝る前（任意）
  ↓ Today の「今日の記録」を見て、一言メモを追加してもしなくてもよい
  ↓ 次ステップが空なら追加（AIが「次はこれどうですか」と提案）
```

---

## 4. 現行コードからの移行方針

### 4.1 残すもの

- **Capture の入力フォーム + リスト UI**: ほぼそのまま使える
- **AiService の基本構造**: HTTP 通信・JSON パースのベース
- **suggestRelatedNodeIds の発想**: Memo → Project 関連候補に転用
- **Hilt / Room / Compose の基盤**: 変更不要
- **AppContent の BottomNav 構造**: タブ名とアイコンを変えるだけ

### 4.2 書き換えるもの

- **NodeEntity → MemoEntity + StepEntity に分割**
- **EdgeEntity → 削除**
- **ThemeEntity → ProjectEntity に拡張**
- **DailyLogEntity を新設**
- **GraphScreen → ProjectsScreen に全面書き換え**
- **TodayScreen → フォーカスプロジェクト中心のビューに書き換え**
- **GraphViewModel → ProjectsViewModel に書き換え**
- **TodayViewModel → フォーカスプロジェクト + DailyLog 対応に書き換え**

### 4.3 削除するもの

- **EdgeDao, EdgeEntity, EdgeType**: DAG 廃止
- **NodeStatus の READY/ACTIVE/DEFERRED/ABANDONED**: ステータスマシンの単純化
- **ThemeViewModel**: Graph 固有の機能
- **recalculateReady()**: READY 判定のロジック全体
- **AiService.rankReadyNodes()**: READY ベースの機能
- **AiService.suggestThemeNames()**: テーマ名提案

### 4.4 実装順序の推奨

```
Phase 1: データモデル移行（DB migration）
  MemoEntity, ProjectEntity, StepEntity, DailyLogEntity を定義
  旧 NodeEntity からのマイグレーション
  
Phase 2: Capture 画面の修正
  Node → Memo に差し替え
  「プロジェクトに紐付ける」操作を追加
  
Phase 3: Projects 画面の新規実装
  プロジェクト一覧 + 詳細（ステップ管理）
  
Phase 4: Today 画面の書き換え
  フォーカスプロジェクト表示 + DailyLog
  
Phase 5: AI 機能の調整
  Memo → Project 関連候補
  Today のステップ提案
```

---

## 5. docs/app-spec.md との関係

app-spec.md の設計は **方向としては正しい** が、以下を簡略化すべき:

| app-spec.md の要素 | 提案での扱い | 理由 |
|---|---|---|
| ScheduledStep | 不要 | 時刻管理は目標スタイルに合わない |
| 通知 | 不要 | 「気が向いたらやる」に通知は過剰 |
| 振り返り3択評価 | DailyLog で代替 | 3択すら面倒。記録が残ればよい |
| AI 4つの役割 | 2つに絞る | 全部作る前に2つを磨く |
| 4タブ | 3タブ | 振り返りは Today に統合 |
| ステップの重さ | 不要 | 管理対象を増やさない |
| テーマの重さ | 不要 | 同上 |
| CoachingMessage | 不要（当面） | AI対話ログの管理は後回し |

---

## 6. CLAUDE.md からの変更点まとめ

CLAUDE.md を改訂する場合の主要な差分:

1. **データモデル**: Node/Edge/Theme → Memo/Project/Step/DailyLog
2. **フロー構造**: Capture→Graph→Today → Capture→Projects→Today
3. **DAG/READY判定**: 廃止。フォーカスプロジェクトの概念で代替
4. **Graph タブ**: Projects タブに置換。エッジ操作ではなくプロジェクト・ステップ管理
5. **ステータスフロー**: 6段階 → Memo は紐付けの有無、Step は pending/done のみ
6. **AI機能**: 3つ→2つ。テーマ名提案とランキングを削除、関連候補と次ステップ提案に集中

---

## 7. リスクと注意点

### 7.1 「また作り直し」にならないために
- Phase 1-2 で Capture が動く状態を先に作り、**使いながら** Phase 3-4 を進める
- 設計を固めすぎてから作り始めない。Capture → Projects の順で実使用しながら調整する
- app-spec.md / implementation-plan.md / tasks.md は一旦凍結し、新しい設計文書を1本にまとめる

### 7.2 データ移行
- 既存の Node データは Memo に移行可能（title → text, body → text の結合）
- Theme は Project にカラム追加で移行可能
- Edge データは移行不要（使われていないため）

### 7.3 複雑さの閾値
以下を導入したくなったら一度立ち止まること:
- ステップの依存関係（DAG の再導入）
- 時刻ベースのスケジューリング
- 通知システム
- 振り返り評価のバリエーション増加
- AI対話の会話履歴管理

**「管理が面倒」がこのアプリの最大の敵。** 機能を足すたびにこの敵が強くなる。

---

## 8. 追加検討事項（2026-04-16 ヒアリング追記）

### 8.1 Capture にアイデア・ステップ・期限付きタスクが混在する問題

**結論: 混在してよい。Capture 時点では分類しない。**

Capture は「頭から出す」フェーズであり、入力時に「これはアイデアか？ステップか？タスクか？」と考えさせた瞬間にフリクションが生まれる。
投入はすべて Memo として統一し、**分類は整理フェーズ（Capture タブでのメモタップ操作 or Projects タブ）で行う** のが正しい。

ただし、整理フェーズで「これは期限付きタスクだ」と気づいたときの受け皿が必要。

#### 期限付きタスクの扱い方針

期限付きタスクは本アプリのコアターゲットではない（Todoist 等が得意な領域）。
しかし、メモに「確定申告 3月まで」のような期限情報が混ざるのは自然なので、最小限の対応をする:

- **Step に `dueDate: String?` を追加** する（任意フィールド）
- dueDate が設定されたステップは Today 画面で期限表示を出す（「あと3日」など）
- **期限管理の高度な機能（リマインダー、カレンダー連携、期限切れ警告）は作らない**
- 厳密なタスク管理が必要なものは外部ツール（Todoist 等）に任せる方針を明示する

```
Step（改訂）
  id: Long
  projectId: Long
  title: String
  sortOrder: Int
  status: pending | done
  dueDate: String?     // "2026-04-30" 形式。任意。
  doneAt: Long?
  createdAt: Long
```

**整理時のフロー:**
```
メモをタップ
  ├→ 「プロジェクトに紐付ける」→ Memo として紐付け（アイデア的なもの）
  ├→ 「ステップにする」→ 指定プロジェクトの Step に変換（具体的な行動）
  └→ 「新規プロジェクトを作る」→ Project 作成 + Memo 紐付け
```

Memo → Step への変換パスを用意することで、「最初はアイデアだったけど具体化した」ケースにも対応できる。

### 8.2 フォーカスプロジェクトの複数選択とAI提案

**フォーカスは複数可（目安3つ）。AIがソフトに提案する。**

#### 設計変更

- `focusedAt: Long?` は変更なし。**複数の Project が同時に focusedAt != null を持てる**
- ハードリミットは設けない（3は目安であって制限ではない）
- フォーカスの上限はAIが「3つ以上になってますが大丈夫ですか？」と聞く形で制御

#### AI のフォーカス提案（3.4 の AI 役割を改訂）

| 優先度 | タイミング | 内容 | 実装難度 |
|--------|-----------|------|----------|
| 高 | Capture 時 | メモ → 既存プロジェクトとの関連候補 | 低（現行流用） |
| 高 | Today 表示時 | フォーカスPJの中から「今日はこれが良さそう」 | 低（現行流用） |
| 中 | Projects 一覧 | フォーカス提案・放置プロジェクトの休止提案 | 中 |

**AI フォーカス提案の具体像:**

Projects 画面で以下のような提案を表示する:

```
💡 AIの提案:
「英語学習」がここ2週間動いていません。休止しますか？
「副業リサーチ」と「LP作成」は関連が強そうです。今週はセットで集中してみては？
```

提案の入力情報:
- 各プロジェクトの最終ステップ完了日
- フォーカス中プロジェクト数
- プロジェクトに紐づくメモ・ステップの内容（関連性判定用）

提案のトーン:
- **「やれ」ではなく「どうですか？」** のニュアンス
- 休止を勧めるときは「一旦お休みにしますか？」くらいの軽さ
- ユーザーが無視しても繰り返し出さない（1週間に1回程度）

#### Today 画面でのフォーカス表示

フォーカスが複数の場合、Today 画面はプロジェクト別にセクション表示:

```
── 副業リサーチ（フォーカス中）
   □ 競合サービスを3つ調べる
   □ 収益モデルをメモする

── LP作成（フォーカス中）
   □ ワイヤーフレームを手書きする

── 今日の記録
   ✓ 競合サービスを3つ調べる（14:30）
   + 自由記録を追加...
```

AIは「今日はこのプロジェクトのこのステップから始めるのが良さそうです」と1つだけハイライトする。
**全部やれとは言わない。1つだけ推す。** これが着手ファーストの体現。

### 8.3 グラフモデルの再評価 — 裏に隠して活かす

#### 元の設計意図は正しかった

DAG で管理するという案の根拠:
- タスクの粒度はバラバラ（アイデアも行動も混在する）
- 融合・分裂する（1つが3つに分かれる、3つが1つにまとまる）
- 時間的・論理的な依存関係がある
- フォルダ階層（単一親）では表現しきれない

**これらはすべて正しい。** 問題はグラフモデル自体ではなく、「グラフ編集をユーザーに露出した UI」にあった。

#### 方針: グラフは DB に持つが、UI はリスト

```
ユーザーが見る世界              裏側のデータモデル
─────────────────              ─────────────────
メモを投入                   →  Memo ノード作成
「これ関連してそう」をAIが提示  →  Edge 自動生成（RELATED）
メモをプロジェクトに紐付け      →  Memo → Project Edge
「これやらないとできない」      →  PREREQUISITE Edge
ステップ完了                  →  依存先が READY に昇格
```

- **DB には Edge テーブルを残す**（セクション 4.3 の「削除」を撤回）
- **UI はリスト・カード・プロジェクト画面** で見せる
- **グラフの構築は AI + 軽い手動操作** が担う（グラフエディタは作らない）
- **Edge を活用した READY 判定は復活** させる

#### UI での Edge 表現（グラフエディタではない）

**Projects 画面のステップ一覧:**
```
── LP作成
   □ ワイヤーフレームを手書きする
   □ デザインをFigmaで作る
       ↑ 前提: ワイヤーフレームを手書きする
   □ コーディングする（前提未完了）
       ↑ 前提: デザインをFigmaで作る
```

- 前提関係は「このステップの前にやること」として自然言語で表示
- 前提の追加: ステップ長押し →「先にやることを設定」→ 同プロジェクト内のステップを選択
- Edge タイプの選択は不要（PREREQUISITE 一択。RELATED は AI が裏で管理）

**Today 画面:**
- 前提未完了のステップはグレーアウト or 非表示
- 「なぜできないか」はタップで確認可能

### 8.4 ステップ溢れ問題 — Icebox による棚上げ

#### 問題

プロジェクトにステップを追加し続けると、同一プロジェクト内で:
- すぐやるもの
- いつかやりたいもの
- やるか不明なもの

が混在して **リストが肥大化し、見る気を失う** 。
これは従来の TODO アプリでも頻発する問題であり、このアプリの設計理念（「リストを増やさない」）に反する。

#### 解決: Step に visibility の概念を導入する

```
Step.visibility: next | icebox
```

| visibility | 意味 | 表示場所 |
|-----------|------|---------|
| next | 近いうちにやる。Today 候補 | Projects 詳細のメインリスト、Today 画面 |
| icebox | いつかやるかも。今は凍結 | Projects 詳細の折りたたみセクション |

- 新規追加されたステップのデフォルトは `next`
- `next` が多すぎる場合（目安5個超）、AI が「いくつか icebox に移しませんか？」と提案
- `icebox` のステップは Today 画面には **一切出ない**
- Projects 詳細画面では「Icebox（12件）」のように折りたたんで表示
- icebox → next への復帰はワンタップ

#### AI による優先度提案

ステップの並び順（sortOrder）を AI が提案する形で優先度を扱う。
**priority カラムは追加しない。** 「高・中・低」のラベル管理は面倒の元。

AIの提案場面:
- Projects 詳細で next ステップが増えたとき: 「この順番でやるのが良さそうです。変えますか？」
- 完了時: 「次はこれが良さそうです」（ステップを上に移動する提案）
- 定期的: 「○○はずっと next にありますが、icebox にしますか？」

```
── 副業リサーチ
   next（3件）
   1. □ 競合サービスを3つ調べる        ← AI推奨: 「まずこれから」
   2. □ 収益モデルをメモする
   3. □ ターゲット層を仮決めする

   icebox（8件）▶              ← 折りたたみ。タップで展開
      □ 法人化の条件を調べる
      □ 確定申告の流れを確認する
      ...
```

#### なぜ priority カラムではなく visibility + sortOrder か

| 方式 | メリット | デメリット |
|------|---------|-----------|
| priority (高/中/低) | 直感的 | 全部「高」にしがち。管理が増える |
| sortOrder のみ | シンプル | 数が多いとスクロールが辛い |
| **visibility + sortOrder** | **見えるものだけ少数。優先順はAIが提案** | **icebox の棚卸しが必要（AIが促す）** |

visibility は **「今見るリストの長さを制御する」** ためのもの。
priority は「重要度のラベルを貼って管理する」もの。
このアプリの思想（管理を減らす）には前者が合う。

### 8.5 データモデル最終版

8.1〜8.4 を統合:

```
Memo
  id: Long
  text: String
  createdAt: Long
  projectId: Long?       // 紐付け先（整理済みフラグ兼用）

Project
  id: Long
  title: String
  goal: String?          // 任意
  status: active | paused | archived
  focusedAt: Long?       // != null ならフォーカス中。複数可（目安3つ）
  createdAt: Long

Step
  id: Long
  projectId: Long
  title: String
  sortOrder: Int          // 表示順。AI が提案、ユーザーが調整
  status: pending | done
  visibility: next | icebox   // next = 今やる候補、icebox = 凍結
  dueDate: String?        // 任意。期限があるステップ用
  doneAt: Long?
  createdAt: Long

Edge
  fromId: Long            // Step or Memo の ID
  toId: Long              // Step or Memo の ID
  type: PREREQUISITE | RELATED
  createdAt: Long

DailyLog
  id: Long
  date: String            // "2026-04-16"
  stepId: Long?
  what: String
  note: String?
  createdAt: Long
```

**セクション 3 からの主要な差分:**

| セクション 3 の提案 | 最終版 | 変更理由 |
|---|---|---|
| Edge テーブル削除 | **復活** | グラフモデルは正しい。UI を変えれば活きる |
| Step は pending/done のみ | **visibility (next/icebox) 追加** | ステップ溢れ防止 |
| READY 判定なし | **Edge ベースで復活** | 依存関係は本質的に有用 |
| AI 2機能 | **3機能（+ ステップ整理提案）** | icebox 提案・並び順提案を追加 |

---

## 9. 設計の検証 — 3つの問いに対する正直な評価

### 9.1 TaskChute のしんどさから脱却できているか

**概ね Yes。ただし1つ注意点がある。**

TaskChute の負荷の正体:
- 全行動を時間単位で記録する義務感
- 見積もり vs 実績の管理
- 日次ルーチンの設計とメンテナンス
- 「記録しないと意味がない」というプレッシャー

提案設計が回避しているもの:

| TaskChute の負荷 | 提案設計 |
|---|---|
| 時刻管理 | なし。「気が向いたらやる」 |
| 見積もり時間 | なし |
| ルーチン管理 | なし |
| 全行動の記録義務 | DailyLog は完了時に自動生成 + 自由記録は任意 |
| 日次レビュー必須 | 「今日の記録」は見るだけ。評価（3択等）も不要 |

**注意点: DailyLog が「記録しなきゃ」プレッシャーにならないこと。**
ステップ完了時の自動記録がメインで、自由記録は「書いても書かなくてもいい」を徹底する。
Today 画面に「今日の記録」セクションを常時表示するかは検討の余地がある（空だとプレッシャーになる可能性）。
→ **DailyLog セクションは記録がある日だけ表示する** のが安全。

### 9.2 既存の TODO アプリとの差別化は図れているか

**部分的に Yes。ただし正直に言うと、構造だけでは差別化が弱い。**

#### Todoist と比較した場合

| 機能 | Todoist | 提案設計 |
|------|---------|---------|
| プロジェクト > タスク | ある | Project > Step（ほぼ同じ） |
| 優先度 | p1-p4 | visibility (next/icebox)（発想は違うが効果は近い） |
| フィルタ/ビュー | 「今日」「近日中」 | フォーカスプロジェクト（似ている） |
| 期限管理 | 強力 | 最小限（差別化ではなく割り切り） |

**構造だけ見ると「シンプルな Todoist」に見えてしまうリスクがある。**

#### 本質的な差別化ポイント（これがないと Todoist で十分）

1. **Memo = 「まだタスクにすらなっていないもの」の居場所**
   - Todoist にはない。入れた瞬間にタスクになる。
   - このアプリでは「ふわっとしたもの」が Memo として安全に溜まり、**タスク化のプレッシャーなく寝かせておける**
   - これが「ふわっとしたアイデアに弱い」という既存 TODO アプリの弱点への直接の回答

2. **グラフによる依存関係（裏で動く READY 判定）**
   - 「これをやらないとあれができない」が自動で反映される
   - Todoist にはこれがない（サブタスクはあるが依存関係ではない）
   - **AI なしでも手動で前提設定すれば動く**

3. **「整理セッション」という概念**
   - Todoist は「追加して消化する」ツール。整理は自分でやる
   - このアプリは **Capture（発散）→ Projects（整理）→ Today（実行）** の3フェーズを明示的に分けている
   - 整理フェーズが設計に組み込まれていること自体が差別化

4. **AI によるソフトコーチング**（後述するが、これが最大の差別化候補）

#### 差別化を確実にするために強調すべきこと

- **Memo の「寝かせておける」性質** をUXで明示する（Capture タブが「アイデアの安全な置き場」であることを伝える）
- **Todoist 的な「タスクを全部見せる」表示をしない** こと。フォーカス + next のフィルタリングを徹底する
- **グラフの恩恵**（前提未完了のステップをグレーアウト）が目に見える形で機能すること

### 9.3 AI 必須でない設計になっているか

**Yes。AI なしで全機能が動く。ただし AI なしだと「差別化ポイントの半分が消える」。**

#### AI なしで動く部分

| 機能 | AI あり | AI なし |
|------|---------|--------|
| メモ投入 | 同じ | 同じ |
| メモ → プロジェクト紐付け | 「○○に関連しそう」候補表示 | 手動で選択 |
| ステップ追加 | — | 同じ（手動） |
| 前提関係の設定 | RELATED を自動提案 | 手動で「先にやること」を設定 |
| Today の表示 | 「今日はこれから」と1つ推奨 | フォーカスPJの next ステップ一覧 |
| icebox 提案 | 「これ凍結しませんか？」 | 手動で icebox に移動 |
| フォーカス提案 | 「今週はこれに集中しては？」 | 手動でフォーカス設定 |
| 並び順 | AI が提案 | 手動で並び替え |

**すべての操作に手動のフォールバックがある。AI は「提案 → 承認」パターンで、AI がなければ「ユーザーが自分で操作」に戻るだけ。**

#### AI なしの場合の懸念

1. **Edge がほとんど作られない**
   - RELATED Edge は AI の自動提案がないと誰も張らない
   - PREREQUISITE は手動でも張るが、面倒なら張らない
   - → グラフの恩恵（READY 判定）が薄くなる
   - **対策**: AI なしでも PREREQUISITE だけは軽い操作で設定できるUIにしておく

2. **icebox の棚卸しが起きない**
   - AI 提案がないと、icebox に入れたものは永久に凍結される
   - **対策**: Projects 詳細画面で「Icebox（12件）最終確認: 30日前」のように経過を表示

3. **差別化ポイントが減る**
   - AI なしだと差別化は「Memo の居場所」「グラフ」「整理フェーズの明示」の3つ
   - これだけでも Todoist とは違うが、**「ちょっと変わった TODO アプリ」感は否めない**
   - AI を入れて初めて「コーチ付きのプロジェクト管理」になる

#### 結論: AI は必須ではないが、コア体験を変える

```
AI なし: Memo + Project + Step + Graph = 「構造化された TODO アプリ」
AI あり: 上記 + ソフトコーチング        = 「個人プロジェクトの伴走者」
```

設計上は AI 非依存が確保されている。
しかし **AI こそがこのアプリを「もう1つの TODO アプリ」から「新しいカテゴリ」に押し上げる** 要素であり、
Phase 5（AI 機能）を「おまけ」ではなく **コア体験の一部** として優先度を上げる価値がある。

特に効果が大きいのは:
- **Capture 時の関連候補提案**（整理の手間を劇的に減らす）
- **Today の「まずこれから」推奨**（選択疲れを防ぐ）

この2つは AI の精度が低くても「あるだけで体験が違う」類のもの。

---

## 10. 結論

現行の設計で最も価値があったのは **「サクッとメモできる」体験** 。
それ以外は、操作コストが高いか使われていない。

提案の核心は:
1. **アイデア（Memo）とアクション（Step）を分ける** ことで、リストの混乱を防ぐ
2. **グラフは裏に持ち、UI はリストで見せる** ことで、構造の柔軟性と操作の軽さを両立する
3. **DailyLog で振り返りを自動化** することで、毎日の負荷を最小にする
4. **「フォーカス」の概念（複数可・AI提案）** で、Today に表示される情報を絞る
5. **visibility (next/icebox) でステップの見える量を制御** し、リスト肥大化を防ぐ
6. **Capture 時は分類しない。整理は後から。** 投入のフリクションを最小に保つ
7. **AI は必須ではないが、コア体験を変える。** 特に Capture 関連候補と Today 推奨の2つは Phase 5 で早めに実装する価値がある

**このアプリが「もう1つの TODO アプリ」で終わるか「新しいカテゴリ」になるかは、
Memo の安全な居場所 + グラフの裏活用 + AI のソフトコーチングの3つが噛み合うかにかかっている。**

この方向で Phase 1 から始めれば、既存コードの 6-7 割は書き換えになるが、
**使い続けられるものができる可能性は今までの設計より高い** と考える。
